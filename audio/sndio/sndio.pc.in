prefix=%%PREFIX%%
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: %%NAME%%
Description: %%DESC%%
Version: %%VERSION%%
Libs: -L${libdir} -l%%NAME%%
Cflags: -I${includedir}
