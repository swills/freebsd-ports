--- Makefile.orig	2019-10-26 15:54:10 UTC
+++ Makefile
@@ -1,6 +1,4 @@
 
-CC   := gcc
-CXX  := g++
 LD   := $(CXX)
 
 MSG_PREFIX ?=
@@ -51,8 +49,6 @@ else
 endif
 
 ARCHFLAGS := $(ARCHFLAGS)
-
-OPTFLAGS  ?= -g -O
 
 CFLAGS    += -Wall -Wno-unused-function -Wno-write-strings -Wno-sign-compare $(ARCHFLAGS)
 ifneq ($(findstring arm,$(shell uname -m)),)
