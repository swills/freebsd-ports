variables:
  GIT_STRATEGY: fetch

before_script:
  - sudo poudriere ports -d $CI_PROJECT_NAMESPACE-$CI_PROJECT_NAME
  - sudo poudriere ports -c -m null -p $CI_PROJECT_NAMESPACE-$CI_PROJECT_NAME -M $CI_PROJECT_DIR

build:
  stage: build
  script:
    - ./gitlab-build.sh

trigger_build:
  stage: deploy
  script:
    - "curl -X POST -F token=63297a2b35a73e4349fde91653ae4a -F ref=devel https://gitlab.mouf.net/api/v4/projects/45/trigger/pipeline"
