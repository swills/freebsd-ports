--- Makefile.libretro.orig	2021-02-19 03:49:31 UTC
+++ Makefile.libretro
@@ -115,7 +115,6 @@ ifeq ($(platform), unix)
 	EXT    ?= so
    TARGET := $(TARGET_NAME)_libretro$(PLAT).$(EXT)
 	fpic := -fPIC
-	LTO ?= -flto=auto 
 	SHARED := -shared -Wl,--version-script=libretro/link.T
 ifneq ($(findstring Haiku,$(shell uname -a)),)
 		LIBM :=
